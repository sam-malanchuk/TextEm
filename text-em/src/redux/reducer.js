// import action types
// import {

// } from './actions'

// state initial values
const initialState = {
}

export default function(state = initialState, action) {
	switch (action.type) {
        default:
            return state
    }
}